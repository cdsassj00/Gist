{
  "name": "gpt-realtime-translate-interpreter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check api/health.js && node --check api/translation-session.js && node --check lib/realtime-session.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
